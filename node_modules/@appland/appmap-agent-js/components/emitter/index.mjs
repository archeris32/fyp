
      import { selectComponentInstance } from "../loader.mjs";
      const { URL } = globalThis;
      const { search, searchParams: params } = new URL(import.meta.url);
      export const {
        closeEmitter, openEmitter, requestRemoteEmitterAsync, sendEmitter, takeLocalEmitterTrace
      } = await import(`./${selectComponentInstance(
        "emitter",
        {"local":["test","node","browser"],"remote-socket":["node","browser"]},
        params,
      )}/index.mjs${search}`);
    