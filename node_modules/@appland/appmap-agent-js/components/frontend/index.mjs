
      import { selectComponentInstance } from "../loader.mjs";
      const { URL } = globalThis;
      const { search, searchParams: params } = new URL(import.meta.url);
      export const {
        createFrontend, formatAfterAmend, formatAfterEvent, formatApplyPayload, formatAwaitPayload, formatBeforeAmend, formatBeforeEvent, formatBeginAmend, formatBeginEvent, formatEndAmend, formatEndEvent, formatError, formatGroup, formatGroupPayload, formatQueryPayload, formatRejectPayload, formatRequestPayload, formatResolvePayload, formatResponsePayload, formatReturnPayload, formatStartTrack, formatStopTrack, formatThrowPayload, formatUngroupPayload, formatYieldPayload, getAnswerPayload, getBundlePayload, getFreshTab, getJumpPayload, getResumePayload, getSerializationEmptyValue, instrument
      } = await import(`./${selectComponentInstance(
        "frontend",
        {"default":["test","node","browser"]},
        params,
      )}/index.mjs${search}`);
    