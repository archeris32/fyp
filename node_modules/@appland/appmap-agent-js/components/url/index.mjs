
      import { selectComponentInstance } from "../loader.mjs";
      const { URL } = globalThis;
      const { search, searchParams: params } = new URL(import.meta.url);
      export const {
        getUrlBasename, getUrlExtension, getUrlFilename, toAbsoluteUrl, toDirectoryUrl, toRelativeUrl
      } = await import(`./${selectComponentInstance(
        "url",
        {"default":["node","test","browser"]},
        params,
      )}/index.mjs${search}`);
    