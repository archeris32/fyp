
      import { selectComponentInstance } from "../loader.mjs";
      const { URL } = globalThis;
      const { search, searchParams: params } = new URL(import.meta.url);
      export const {
        convertFileUrlToPath, convertPathToFileUrl, fromIpcPath, getCwdPath, getCwdUrl, getPathBasename, getPathExtension, getPathFilename, getShell, getTmpPath, getTmpUrl, sanitizePathFilename, toAbsolutePath, toDirectoryPath, toIpcPath, toRelativePath
      } = await import(`./${selectComponentInstance(
        "path",
        {"node":["test","node"]},
        params,
      )}/index.mjs${search}`);
    