
      import { selectComponentInstance } from "../loader.mjs";
      const { URL } = globalThis;
      const { search, searchParams: params } = new URL(import.meta.url);
      export const {
        closeAgent, formatApplyPayload, formatAwaitPayload, formatGroupPayload, formatQueryPayload, formatRejectPayload, formatRequestPayload, formatResolvePayload, formatResponsePayload, formatReturnPayload, formatThrowPayload, formatUngroupPayload, formatYieldPayload, getAnswerPayload, getBundlePayload, getFreshTab, getJumpPayload, getResumePayload, getSerializationEmptyValue, instrument, openAgent, recordAfterAmend, recordAfterEvent, recordBeforeAmend, recordBeforeEvent, recordBeginAmend, recordBeginEvent, recordEndAmend, recordEndEvent, recordError, recordGroup, recordStartTrack, recordStopTrack, requestRemoteAgentAsync, takeLocalAgentTrace
      } = await import(`./${selectComponentInstance(
        "agent",
        {"default":["test","node","browser"]},
        params,
      )}/index.mjs${search}`);
    