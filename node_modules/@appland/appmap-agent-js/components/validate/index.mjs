
      import { selectComponentInstance } from "../loader.mjs";
      const { URL } = globalThis;
      const { search, searchParams: params } = new URL(import.meta.url);
      export const {
        validateExternalConfiguration, validateInternalConfiguration, validateMessage, validatePayload, validateSerial, validateSourceMap
      } = await import(`./${selectComponentInstance(
        "validate",
        {"ajv":["test","node","browser"]},
        params,
      )}/index.mjs${search}`);
    