
      import { selectComponentInstance } from "../loader.mjs";
      const { URL } = globalThis;
      const { search, searchParams: params } = new URL(import.meta.url);
      export const {
        AssertionError, assert, assignProperty, bind, bindEither, coalesce, coalesceCaseInsensitive, compose, constant, createBox, createCounter, decrementCounter, format, fromEither, fromLeft, fromMaybe, fromRight, gaugeCounter, generateDeadcode, generateGet, getBox, getOwnProperty, hasOwnProperty, identity, incrementCounter, isLeft, isRight, jsonifyNumber, makeLeft, makeRight, mapEither, mapMaybe, matchVersion, memoize, noop, print, recoverMaybe, returnFirst, returnSecond, returnThird, setBox, setOwnProperty, spyOnce, toBoolean, toNumber, toString, zip
      } = await import(`./${selectComponentInstance(
        "util",
        {"default":["test","node","browser"]},
        params,
      )}/index.mjs${search}`);
    