
      import { selectComponentInstance } from "../loader.mjs";
      const { URL } = globalThis;
      const { search, searchParams: params } = new URL(import.meta.url);
      export const {
        compileConfigurationCommand, extendConfigurationNode, extendConfigurationPort, getConfigurationPackage, getConfigurationScenarios, isConfigurationEnabled, resolveConfigurationAutomatedRecorder, resolveConfigurationManualRecorder, resolveConfigurationRepository
      } = await import(`./${selectComponentInstance(
        "configuration-accessor",
        {"default":["test","node","browser"]},
        params,
      )}/index.mjs${search}`);
    