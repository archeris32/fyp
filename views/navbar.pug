style.
    label{
        margin: 20px;
    }
    .cont ul {
        opacity: 0;
        visibility: hidden;
        text-align: left;
        transition: all 200ms;
    }
    .cont:hover ul {
        opacity: 1;
        visibility: visible;
    }
    .cont ul {
        position: absolute;
        padding: 4px 0;
        top: 28px;
        right: 0;
        width: 80px;
        background-color: white;
        border: 1px solid #ccc;
        border-radius: 4px;
        box-shadow: 0 3px rgba(0, 0, 0, 0.05);
        list-style-type: none;
    }
    .cont:hover ul:before {
        content: '';
        position: absolute;
        top: -12px;
        height: 12px;
        width: 100%;
        background-color: transparent;
    }
    .cont:hover ul:after {
        content: '';
        position: absolute;
        right: 6px;
        top: -16px;
        height: 0;
        width: 0;
        border: 8px solid transparent;
        border-bottom-color: #ccc;
    }
    .cont:hover ul li {
        padding: 5px 10px;
        color: #999;
        cursor: pointer;
        transition: all 100ms;
    }
    .cont:hover ul li:hover {
        background-color: #e6e6e6;
        color: black;
    }
    .cont:hover ul li:first-of-type:after {
        content: '';
        position: absolute;
        right: 6px;
        top: -15px;
        height: 0;
        width: 0;
        z-index: 1;
        border: 8px solid transparent;
        border-bottom-color: white;
        pointer-events: none;
    }
    .cont {
        margin: 0;
    }
    .container{
        width: 100%;
        padding: 0;
    }
  

nav(class="navbar navbar-expand-sm d-flex mx-auto d-flex flex-wrap align-items-center justify-content-center  mb-5")
    div(class="collapse navbar-collapse" style="float:left;")
        ul(class="navbar-nav") 
            li(class="nav-item active custom-nav-item")
                a(href="/" class="nav-link text-dark custom-nav-link") Home
            li(class="nav-item active custom-nav-item")
                a(href="/about" class="nav-link text-dark custom-nav-link") About
            li(class="nav-item active custom-nav-item")
                a(href="/services" class="nav-link text-dark custom-nav-link") Services
            li(class="nav-item active custom-nav-item")
                a(href="/team" class="nav-link text-dark custom-nav-link") Team   
            li(class="nav-item active custom-nav-item")
                a(href="/contact" class="nav-link text-dark custom-nav-link") Contact
            li(class="nav-item active custom-nav-item")
                a(href="/booking" class="nav-link text-dark custom-nav-link") Book Appointment
            if (username)
                li(class="nav-item active custom-nav-item")
                    a(href="/contact_admin" class="nav-link text-dark custom-nav-link") Contact Administrator

    a(href="/" style="height: 50px;width: 100px;margin:auto;display: block;position: absolute;left: calc((100% - 100px) / 2);z-index: 100000;")
        img(src="https://i.ibb.co/5TL93Kc/zentist-high-resolution-logo-black-on-transparent-background.png"  alt="logo" width=100)
    div(class="col-md-3 text-end justify-content-end" bis_skin_checked="1")
        a(type="button" href=username ? "/logout" : "/login" class="btn btn-outline-primary me-2" style= username&& 'display:none') Login
        a(type="button" href="/register" class="btn btn-primary" style=username&& 'display:none') Sign up
    if(username)
        a(type="button" href="/results" class="btn btn-outline-primary me-2") Dashboard
        if(username)
            div(class="cont justify-content-end")
                label(class="name") #{username}
                    ul
                        li 
                            a(href="/profile") Profile
                            #{username}
                        li 
                            a(href="/logout") Logout
    