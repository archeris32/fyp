extends default

block content
   form(action="/edit_client" method="POST")
        div(class="md-form mt-0 w-25 p-3")
            p Name
            input(class ="form-control" type="text" placeholder="search" aria-label="search" name="searchName" required)
        button(class="btn btn-primary" type="submit") Submit form
                        
        button(class="btn btn-primary" onclick="location.href='results'") Go Back